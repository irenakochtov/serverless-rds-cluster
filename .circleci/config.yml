version: 2.1

executors:
  terraform-executor:
    docker:
      - image: hashicorp/terraform:1.6.6
    working_directory: ~/project/serverless_rds_cluster

jobs:
  deploy:
    executor: terraform-executor
    steps:
      - checkout

      - run:
          name: Debug - Show Terraform Files
          command: |
            cd terraform
            ls -la

      - run:
          name: Initialize Terraform
          command: |
            cd terraform
            terraform init

      - run:
          name: Apply Terraform using tfvars
          command: |
            cd terraform
            terraform apply -auto-approve -var-file="terraform.tfvars"

workflows:
  deploy_workflow:
    jobs:
      - deploy